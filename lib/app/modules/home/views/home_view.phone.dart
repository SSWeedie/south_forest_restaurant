import 'package:flutter/material.dart';

class HomeViewPhone extends StatelessWidget {
  const HomeViewPhone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
